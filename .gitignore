# See http://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
node_modules

# testing
coverage

# production - we are using gh-pages so we need the build
# build

# misc
.DS_Store
.env
npm-debug.log
